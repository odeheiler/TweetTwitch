<Window x:Class="TweetTwitch.Views.PinView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PinView" Height="100" Width="200">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal" >
            <Label Width="60"
                   HorizontalAlignment="Left">Enter Pin:</Label>
            <TextBox Width="100" Text="{Binding Twitter.Pin, UpdateSourceTrigger=PropertyChanged}" 
                     
                      />
        </StackPanel>
            <Button Margin="10" HorizontalAlignment="Left" Width="100" Command="{Binding EnterPinCommand}">Enter</Button>
        </StackPanel>
    </Grid>
</Window>
